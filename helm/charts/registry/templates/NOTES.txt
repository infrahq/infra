Welcome to Infra. It may take a few minutes for your load balanacer to expose an IP address.

To get started, run:

export SERVICE_IP=$(kubectl get svc --namespace infra infra --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
infra login $SERVICE_IP

