{{- if .Values.ingress.enabled -}}
{{- $servicePort := .Values.ingress.grpc.servicePort -}}
apiVersion: {{ include "infra.ingress.apiVersion" . }}
kind: Ingress
metadata:
{{- if .Values.ingress.grpc.annotations }}
  annotations:
  {{- range $k, $v := .Values.ingress.grpc.annotations }}
    {{ $k }}: {{ tpl (toYaml $v) $ }}
  {{- end }}
{{- end }}
  name: infra-grpc
  labels:
    {{- if .Values.ingress.grpc.labels }}
    {{- toYaml .Values.ingress.grpc.labels | indent 4 }}
    {{- end }}
spec:
  rules:
    {{- range $host := .Values.ingress.hosts }}
    - host: {{ $host }}
      http:
        paths:
          - path: /v1.V1
            {{- if eq (include "infra.ingress.apiVersion" $) "networking.k8s.io/v1" }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if eq (include "infra.ingress.apiVersion" $) "networking.k8s.io/v1" }}
              service:
                name: infra
                port:
                  number: {{ $servicePort }}
              {{- else }}
              serviceName: infra
              servicePort: {{ $servicePort }}
              {{- end }}
          - path: /grpc.reflection.v1alpha.ServerReflection
            {{- if eq (include "infra.ingress.apiVersion" $) "networking.k8s.io/v1" }}
            pathType: Prefix
            {{- end }}
            backend:
              {{- if eq (include "infra.ingress.apiVersion" $) "networking.k8s.io/v1" }}
              service:
                name: infra
                port:
                  number: {{ $servicePort }}
              {{- else }}
              serviceName: infra
              servicePort: {{ $servicePort }}
              {{- end }}
    {{- end -}}
  {{- if .Values.ingress.tls }}
  tls:
{{ tpl (toYaml .Values.ingress.tls) $ | indent 4 }}
  {{- end -}}
{{- end -}}
