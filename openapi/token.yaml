Token:
  type: object
  required:
    - token
    - expires
  properties:
    token:
      type: string
    expires:
      type: integer
      format: int64
TokenRequest:
  type: object
  required:
    - destination
  properties:
    destination:
      type: string
      x-go-custom-tag: validate:"required"
