get:
  summary: Get version information
  tags:
    - version
  operationId: version
  responses:
    '200':
      description: Version response
      content:
        application/json:
          schema:
            $ref: ../components/schemas/Version.yaml
