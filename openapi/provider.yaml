Provider:
  type: object
  required:
    - id
    - created
    - updated
    - domain
    - clientID
    - kind
  properties:
    id:
      type: string
    created:
      type: integer
      format: int64
    updated:
      type: integer
      format: int64
    domain:
      type: string
      x-go-custom-tag: validate:"required"
    clientID:
      type: string
      x-go-custom-tag: validate:"required"
    kind:
      type: string
      x-go-custom-tag: validate:"required"
