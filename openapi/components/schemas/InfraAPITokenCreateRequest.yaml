type: object
required:
  - name
  - permissions
properties:
  name:
    type: string
  permissions:
    type: array
    items:
      type: string
  ttl:
    type: string
    description: Token time to live before expiry in the form XhYmZs, for example 1h30m. Defaults to 12h.