Destination:
  type: object
  required:
    - id
    - name
    - created
    - updated
    - labels
  properties:
    id:
      type: string
    name:
      type: string
    created:
      type: integer
      format: int64
    updated:
      type: integer
      format: int64
    labels:
      type: array
      items:
        type: string
    kubernetes:
      $ref: 'destination.yaml#/DestinationKubernetes'
DestinationCreateRequest:
  type: object
  required:
    - name
    - labels
  properties:
    name:
      type: string
    labels:
      type: array
      items:
        type: string
    kubernetes:
      $ref: 'destination.yaml#/DestinationKubernetes'
DestinationKubernetes:
  type: object
  required:
    - ca
    - endpoint
  properties:
    ca:
      type: string
      x-go-custom-tag: validate:"required"
    endpoint:
      type: string
      x-go-custom-tag: validate:"required"
