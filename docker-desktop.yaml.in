logLevel: debug

image:
  tag: $IMAGE_TAG
  pullPolicy: Never

image:
  tag: $IMAGE_TAG
  pullPolicy: Never

engine:
  logLevel: debug
  config:
    name: docker-desktop

  image:
    tag: $IMAGE_TAG
    pullPolicy: Never

  service:
    ports:
      - name: https
        port: 8443
        targetPort: 443
