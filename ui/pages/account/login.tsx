import styled from "styled-components";

const LoginContainer = styled.section`
  padding: 0 2rem;
  overflow: hidden;
  max-width: 75rem;
  margin: 0 auto;
`



const Login = () => {
  return (
      <LoginContainer>
        This is Login page
      </LoginContainer>
  )
};

export default Login;